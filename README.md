# HotMicCore (Android)

Customer-facing HotMic Core Android distribution. Example app and docs live here; the SDK
artifact is published separately (`io.hotmic.core:hotmic-core-android`).

Bootstrap commit — full README and Example land via `hem/74-android-core-dist`.
